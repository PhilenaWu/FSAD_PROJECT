# UC-011 — Admin Cost Analytics Dashboard (frontend)

**Owner:** Hasini (UI + analytics engine) · Backend endpoints (`/api/admin/costs/*`): Davian
**Route:** `/admin/costs` · role-gated to `admin` (layout + page check + backend `requireRole`)
**Key files:** `frontend/src/pages/AdminCostPage.jsx`, `frontend/src/services/costService.js`,
`frontend/src/services/costService.test.js`, `backend/src/controllers/adminController.js`

## Where each number comes from

The page is fully database-backed — there is no mock data source.

| Endpoint | Feeds |
| --- | --- |
| `GET /costs/summary` | The KPI tiles. Total spend, job count and projected exposure are SQL aggregates; the movement tile asks the same endpoint for the prior window. |
| `GET /costs/jobs` | Every row-level panel: the drill-down table and CSV, the category/contractor breakdowns, the monthly trend, the top mover, and the price benchmarks. |
| `GET /costs/filter-options` | The block / category / contractor dropdowns — drawn from the costed rows, so no option can lead to an empty view. |

## Architecture decision: pure functions over raw rows

The analytics themselves live as **pure, exported functions** in `costService.js` that operate
on the job-level rows the server returns. The server owns filtering and the money totals; the
pure functions own what SQL does not compute (grouping with counts, trend gap-fill, forecast,
backtest, watchlist, benchmarks, insight sentences).

Why:

- **Everything is unit-testable** with hand-checkable fixtures — no component rendering, no
  network mocking. 43 Vitest tests cover the engine.
- **Nothing is canned.** Every number on the page is computed at request time from database
  rows, so filters genuinely recompute all KPIs, charts, insights, and flags.

## Forecasting: why damped-trend exponential smoothing

The trend chart projects three months ahead using **Holt's method with a damping factor**
(α = 0.5, β = 0.3, φ = 0.85) rather than a moving average or plain linear regression:

- A moving average ignores trend direction entirely (a rising history "predicts" its own past).
- Undamped linear extrapolation runs away — three months out it will happily predict spend the
  estate has never seen. Damping (φ < 1) flattens the projected trend as the horizon grows,
  which is the textbook guard for short-horizon operational forecasts.
- The fit uses **complete months only** — the current calendar month is excluded because its
  partial total would drag the trend down.

Each projected point carries a **±1.28σ (~80%) range** derived from the model's own
one-step-ahead in-sample errors, widening with √horizon. The model refuses to project with
fewer than three complete months, and clamps at $0.

## Self-validation: walk-forward backtest

The dashboard grades its own model. For each historical month with ≥3 complete months before
it, the model is **refit on strictly earlier data** and its prediction compared against the
real total. The badge next to the trend chart shows the mean absolute percentage error, and
its tooltip lists every prediction vs actual. Properties enforced by test:

- **No peeking** — a spike in month *m* cannot influence the prediction *for* month *m*.
- Zero-spend months are skipped (percentage error against zero is undefined).
- The partial current month is never graded.
- With too little history the badge disappears rather than fake a score; the chip turns
  amber/red honestly if the model is mediocre.

## Honesty guards elsewhere

| Feature | Guard |
| --- | --- |
| Spend movement KPI | Prior window ends the day *before* the current one starts — a job on the boundary date is never double-counted (regression-tested). |
| Top mover (Insights) | Compares the last two **complete** months, so a partial month can't fake a movement. |
| Contractor "Vs peers" benchmark | Compared **within the same category** only (controls job-mix confounding); requires ≥2 own jobs, ≥2 peer jobs, ≥15% deviation before claiming anything. |
| Lift time-to-review | Rate from the last 6 complete months; shows "—" with no recent spend rather than inventing a date. |
| Outlier chips | Only when a job exceeds 2× its category's average, with the average shown in the tooltip. |
| Insights panel | Deterministic rules engine (no LLM): each sentence has a "worth saying" threshold, e.g. contractor concentration only speaks at ≥40% share. |

## Cross-use-case links

- **UC-004 → UC-011:** closing a job emits `status_update` to `admin-room`; the dashboard
  shows a "job just closed — refresh view?" snackbar (prompt, not auto-refresh, so charts
  don't move mid-read).
- **UC-006 → UC-011:** active AI risk alerts' `estimated_cost` feeds the Projected-exposure
  KPI tile.
- **UC-012 → UC-011:** the cost-per-contractor table (with peer benchmarking) is the evidence
  an admin brings to the vendor renewal decision on the Vendors page.
- **4C-2 pain point:** per-chart PNG download and a print-ready snapshot (controls hidden via
  `displayPrint`) address the manual monthly-reporting complaint directly.

## Test strategy

`costService.test.js` (Vitest, small fixed fixtures — expected values are hand-checkable):
comparison windows incl. the boundary case · grouping · trend gap-fill ·
forecast properties (flat→flat with zero-width band, rising→damped rise, partial-month
exclusion, zero clamp, band widening) · backtest no-peeking and edge cases · top-mover rules ·
benchmark evidence guards · insight sentence thresholds · lift watchlist maths.

The audit that produced this suite caught two real bugs: a window-boundary double count in the
variance calculation, and timezone-fragile month arithmetic in the trend gap-fill (replaced
with pure string/int month math).

## Demo data

`backend/migrations/034_seed_lift_cost_history.sql` seeds the cost history the panels read.
Dates are relative to `CURRENT_DATE`, so the window stays correct whenever it is applied, and
the file is idempotent (fixed primary keys + `ON CONFLICT DO NOTHING`). Every seeded row is a
closed lift rectification joined to a real `lifts` row by `lift_code`, so lift, block and
contractor always agree with the estate that is loaded.

What it produces on the dashboard (verified against the database):

| Panel | Result |
| --- | --- |
| KPI tiles | $268,600 across 255 closed jobs · spend movement **+2.8%** vs the prior 90 days · $7,968.71 projected exposure |
| Monthly spend trend | 13 months, gently rising; 3-month projection ~$25.0k–$25.8k with an ~80% band |
| Backtest chip | **±5.8% average error over 9 months** (green) |
| Lift watchlist | 44A-L1 at 116% of the threshold (*Review replacement*), 44B-L1 at 79% (*Approaching review*, ~4 months), six below 55% |
| Cost per contractor | 6 contractors, with "Vs peers" deviations flagged inside categories |
| Recent costed jobs | 5 outlier jobs — major overhauls priced above 2× their category average |

To remove it: `DELETE FROM inspections WHERE id::text LIKE 'd0000000-0000-4000-8000-%';`

## Outstanding

1. Visual pass with a production admin login.
