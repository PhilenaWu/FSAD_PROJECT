// Supported translation target languages (migration 047), shared by every
// route that accepts a ?lang= — the manager/inspector detail-page translation
// and the resident's MyReportsPage extras translation. Mirrors the frontend's
// own list in frontend/src/utils/languages.js.
'use strict';

const TRANSLATION_LANGUAGES = ['en', 'zh', 'ms', 'ta'];

module.exports = { TRANSLATION_LANGUAGES };
