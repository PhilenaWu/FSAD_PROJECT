// Auth is handled by Supabase on the client (see AGENTS.md) — there are no
// custom auth endpoints to unit-test. Middleware tests live with their routes.
// test.todo keeps the suite green (an empty file fails the whole jest run).
test.todo('auth middleware edge cases (requireAuth / requireRole)');
