// Auth is handled by Supabase on the client (see AGENTS.md) — sign-up/login
// flows have no custom backend endpoints to integration-test here.
// test.todo keeps the suite green (an empty file fails the whole jest run).
test.todo('token verification against protected routes');
